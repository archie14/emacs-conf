;; This configuration file is for Magit - and configures git interoperability from Emacs.

;; Show Magit Status
(global-set-key (kbd "C-x g") 'magit-status)

;;Show Magit Pop-up
(global-set-key (kbd "C-x M-g") 'magit-dispatch-popup)

